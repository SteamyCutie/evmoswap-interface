export enum Chef {
    MASTERCHEF,
}

export enum Rewarder {
    SIMPLE,
    COMPLEX,
    ALCX,
}

export enum PairType {
    SWAP,
    KASHI,
}

export enum OnsenModalView {
    Liquidity,
    Staking,
    Position,
    AddLiquidityConfirm,
    Withdraw,
    Deposit,
}
